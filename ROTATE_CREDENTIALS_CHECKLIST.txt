# 🚨 CREDENTIAL ROTATION CHECKLIST - REQUIRED IMMEDIATELY
# Date: 2025-11-07
# Reason: Credentials were exposed in git history

================================================================================
⚠️  WARNING: These credentials WERE in git history commits:
================================================================================
- Database Password: West@Boca613!
- Supabase Service Role Key: (visible in git history)
- Git commits: bc47dfc, de9847c, 3d6356e

Anyone with access to your repository could have seen these credentials.
You MUST rotate them immediately.

================================================================================
📋 STEP-BY-STEP ROTATION PROCESS
================================================================================

Step 1: Rotate Supabase Database Password (15 minutes)
------------------------------------------------------
1. Go to: https://supabase.com/dashboard
2. Select your ConcordBroker project
3. Go to: Settings → Database
4. Click "Reset Database Password"
5. Generate a strong password (or use: https://1password.com/password-generator/)
6. Save password securely in your password manager
7. Update the following files with new password:
   - .env.mcp (SUPABASE_DB_PASSWORD)
   - Any other .env files that have database connection strings

Step 2: Regenerate Supabase Service Role Key (5 minutes)
---------------------------------------------------------
1. In Supabase Dashboard
2. Go to: Settings → API
3. Under "Service Role Key" section
4. Click "Generate new key" or "Reset key"
5. Copy the new service role key
6. Update the following files:
   - .env.mcp (SUPABASE_SERVICE_ROLE_KEY)
   - apps/api/.env (if exists)
   - Any other .env files with this key

Step 3: Update Railway Environment Variables (10 minutes)
----------------------------------------------------------
1. Go to: https://railway.app
2. Select your ConcordBroker project
3. For each service (API, Meilisearch, etc.):
   a. Click on service
   b. Go to "Variables" tab
   c. Update these variables:
      - SUPABASE_URL (may not need to change)
      - SUPABASE_SERVICE_ROLE_KEY (new key from Step 2)
      - Any database connection strings (new password from Step 1)
   d. Redeploy the service (Railway does this automatically)

Step 4: Update Vercel Environment Variables (10 minutes)
---------------------------------------------------------
1. Go to: https://vercel.com/dashboard
2. Select your ConcordBroker project
3. Go to Settings → Environment Variables
4. Update/verify these variables:
   - VITE_SUPABASE_URL
   - VITE_SUPABASE_ANON_KEY (this should be safe, it's the anon key)
   - Any service role keys (should NOT be in frontend)
5. Redeploy: Deployments → Latest → Redeploy

Step 5: Update Local Development Environment (5 minutes)
---------------------------------------------------------
1. Update all .env files with new credentials:
   □ .env.mcp
   □ apps/web/.env
   □ apps/api/.env (if exists)
   □ Any other .env* files

2. Restart all local services:
   □ Stop all node processes: taskkill /F /IM node.exe
   □ Restart MCP server: node claude-code-ultimate-init.cjs
   □ Restart dev server: npm run dev

Step 6: Test Everything (15 minutes)
------------------------------------
□ Local Development:
  - Open http://localhost:5191
  - Test property search
  - Check console for errors
  - Verify data loads correctly

□ MCP Server:
  - curl http://localhost:3005/health
  - Should return 200 OK

□ Production Site:
  - Open https://www.concordbroker.com
  - Test property search
  - Verify all features work
  - Check for any error messages

□ Railway Services:
  - Check logs for each service
  - Verify no authentication errors
  - Confirm successful database connections

□ API Endpoints:
  - Test a few API calls
  - Verify responses are correct
  - Check for auth errors

Step 7: Verify Old Credentials No Longer Work (5 minutes)
----------------------------------------------------------
□ Try connecting to database with OLD password
  - Should FAIL (this confirms rotation worked)

□ Try API call with OLD service role key
  - Should return 401 Unauthorized

Step 8: Document & Secure (5 minutes)
--------------------------------------
□ Update password manager with new credentials
□ Share new credentials with team (if applicable) via secure channel
□ Document when rotation occurred: 2025-11-07
□ Add reminder to calendar: "Rotate credentials in 90 days"

================================================================================
⏱️  ESTIMATED TOTAL TIME: 60-70 minutes
================================================================================

================================================================================
🔒 SECURITY BEST PRACTICES GOING FORWARD
================================================================================

1. NEVER commit credentials to git
   - Always use .env files
   - Keep .env files in .gitignore
   - Use environment variables in CI/CD

2. Use git hooks to prevent credential commits
   - Install: npm install --save-dev husky
   - Add pre-commit hook to scan for secrets

3. Rotate credentials regularly
   - Every 90 days minimum
   - Immediately if any exposure suspected

4. Use different credentials for different environments
   - Development: different password
   - Staging: different password  
   - Production: different password

5. Implement secrets management
   - Consider: 1Password, AWS Secrets Manager, or Vault
   - Never share credentials in plain text

================================================================================
✅ COMPLETION CHECKLIST
================================================================================

□ Step 1: Rotated Supabase database password
□ Step 2: Regenerated Supabase service role key
□ Step 3: Updated Railway environment variables
□ Step 4: Updated Vercel environment variables
□ Step 5: Updated local .env files
□ Step 6: Tested everything works
□ Step 7: Verified old credentials don't work
□ Step 8: Documented and secured new credentials

Once ALL items are checked, you can mark this task complete.

================================================================================
📞 NEED HELP?
================================================================================

If you encounter issues:
- Supabase docs: https://supabase.com/docs/guides/database/managing-passwords
- Railway docs: https://docs.railway.app/reference/variables
- Vercel docs: https://vercel.com/docs/concepts/projects/environment-variables

Common issues:
- "Connection timeout": Check firewall/IP restrictions
- "Authentication failed": Double-check you copied credentials correctly
- "Service won't start": Check logs for specific error messages

================================================================================
⚠️  DO NOT SKIP THIS PROCESS
================================================================================

These credentials were in git history. You MUST rotate them.
Anyone with access to your repository could compromise your application.

Created: 2025-11-07 09:41 EST
