# Copy this file to "apps/api/.env" and fill in values.
# Lines starting with # are comments.

# ---------------------------
# Redis Configuration
# ---------------------------
# Preferred single URL format (Redis Cloud or single-node):
#   - Non-TLS:  redis://default:<PASSWORD>@<HOST>:<PORT>/0
#   - TLS:      rediss://default:<PASSWORD>@<HOST>:<PORT>/0
REDIS_URL=

# Or provide discrete connection fields instead of REDIS_URL
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_USER=default
# Use 1 for TLS-enabled connections (e.g., Redis Cloud)
REDIS_TLS=0

# ---------------------------
# Supabase Configuration
# ---------------------------
SUPABASE_URL=
SUPABASE_ANON_KEY=
# Optional: service role for admin tasks (do not expose client-side)
SUPABASE_SERVICE_ROLE_KEY=

# ---------------------------
# API Settings
# ---------------------------
# Example extra env for your FastAPI app
# APP_ENV=development
# LOG_LEVEL=info

